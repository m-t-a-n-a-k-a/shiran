[package]
name = "shiran"
version = "0.1.0"
edition = "2024"
description = "A bounded, schema-less snapshot of Linux /proc and /sys."
license = "MIT"

[[bin]]
name = "shiran"
path = "src/main.rs"

[dependencies]
